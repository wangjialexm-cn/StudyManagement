# Day4 物理 Answer

## Class Practice 答案

1. `v=s/t`。`s` 表示总路程或位移，`t` 表示总时间。
2. `v=60/10=6 m/s`。
3. 因为平均速度看的是总路程除以总时间，不是简单平均几个速度数值。
4. 不一定。若各段用时不同，不能直接平均速度。
5. 总路程 `100 m`，总时间 `30 s`，平均速度 `100/30≈3.33 m/s`。
6. `v=100/25=4 m/s`。

## Homework 答案

1. `v=s/t`。
2. `80/20=4 m/s`。
3. `150/30=5 m/s`。
4. `s` 是总路程或位移，`t` 是总时间。
5. `s=100 m`，`t=40 s`，`v=2.5 m/s`。
6. `s=100 m`，`t=20 s`，`v=5 m/s`。
7. 示例：平均速度不是看某一段，而是看全程总路程和总时间。
8. 错。只有在特殊条件下才可能等于速度平均值，通常应使用总路程除以总时间。

